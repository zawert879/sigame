import { Player } from "../src/entity/Player"
import { QueuePlayers } from "../src/entity/QueuePlayers"

describe('Queue players #1', () => {
  // const queuePlayers = new QueuePlayers()
  // it('add and remove players', () => {
  //   const player1 = new Player({} as any)
  //   const player2 = new Player({} as any)
  //   const player3 = new Player({} as any)
  //   queuePlayers.addPlayer(player1)
  //   queuePlayers.addPlayer(player2)
  //   queuePlayers.addPlayer(player3)
  //   expect(queuePlayers.queue.length).toBe(3)
  //   queuePlayers.removePlayer(player2.id)
  //   expect(queuePlayers.queue.length).toBe(2)
  //   queuePlayers.clear()
  //   expect(queuePlayers.queue.length).toBe(0)
  // })
})
