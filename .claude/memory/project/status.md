---
name: project-status
description: Состояние SI Game на 2026-09-19 — ветка fix/audit-2026-09-18, что проверено, что осталось, ближайшие планы
metadata:
  type: project
---

> Обновлено 2026-09-19. Исходный аудит — `docs/AUDIT-2026-09-18.md` (все пункты закрыты, см. раздел «Статус» в нём),
> бэклог — [[plans-improvements]].

## Ветка `fix/audit-2026-09-18` (не влита в `main`)

| Коммит | Что |
|---|---|
| `10597fa` | Все пункты аудита: безопасность, протокол с ack, домен без хаков, SIQ 4, zustand-стор, тесты, Node 24, desktop-сборки, CI |
| `e825f7b` | Зачистка наших комментариев, htmlFile, onUpdateSettings, реплики, защита порта, две ссылки в баннере, fullscreen, план лаунчера |
| `44c69b1` | Переделка вёрстки ТВ и пульта (FitText везде, ТВ помещается в экран, адаптивный пульт), очередь сбрасывается при правильном ответе |

## Проверки (2026-09-19)

- service: `yarn typecheck`, `yarn lint` (включая test/), `yarn test` — 12 наборов, 328 тестов.
- admin: `yarn typecheck`, `yarn lint` — 0 ошибок и предупреждений.
- root: `yarn package:all` (exe + 2 mac-бинарника), `yarn smoke`, `yarn smoke release/sigame-macos-arm64`.
- Вёрстка: headless-съёмка на матрице тестовых паков (тексты до 1500 символов, 2–8 вариантов ответа, медиа всех пропорций,
  табло 12×12, SIQ 4) — 0 находок детектора на ТВ (1920×1080, 1280×720, 1024×768) и 0 ошибок на пульте (390×844, 768×1024, 1440×900).
  Стенд съёмки жил в scratchpad сессии и в репозиторий не сохранён.
- GitHub Actions: build.yml собирает артефакты на каждый push.

## Известные мелочи

- Сервер называет новые игры 'Default' (фронт показывает «Без названия»).
- «Повторить вопрос» запускает медиа на ТВ с начала (reset с isPlaying:true) — осознанное поведение.
- На вертикальном планшете при >4 игроков нижние строки таблицы игроков ниже сгиба (страница прокручивается).
- Некоторые чужие комментарии пользователя остались (`// <--- ИЗМЕНЕНИЕ ЗДЕСЬ` в admin tsconfig) — не трогать.

## Дальше (согласовано с пользователем)

- Целостность паков по `<files>` (SHA-256 в content.xml SIQ 5) и обложка пака (`logo`) на заставке и в списке паков.
- Нативное окно запуска на Tauri 2 — план `docs/LAUNCHER-PLAN.md`, превью `docs/launcher-preview.html`.
- Подпись/нотаризация (Apple Developer ID, сертификат для Windows) — по желанию пользователя.
